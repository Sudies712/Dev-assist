# 端口
VITE_PORT = 80

# 公共路径
VITE_PUBLIC_PATH = /

# 路由模式
VITE_ROUTER_HISTORY = hash

# 是否开启CDN加速
VITE_CDN = false

# 开启打包压缩
VITE_COMPRESSION = none

# 后端 API 基础路径（开发走 vite proxy /api → http://localhost:8080）
VITE_API_BASE_URL = /api
